//用户个人界面路由 因为需要将它包装成容器组件import React,{Component} from 'react'import {connect} from 'react-redux'import {Result,List,WhiteSpace,Button,Modal} from 'antd-mobile'import Cookies from 'js-cookie'import {resetMsg} from '../../redux/actions'const Item = List.Itemconst Brief = Item.Briefclass Personal extends Component{    loginOut = () => {        Modal.alert('退出','确定退出登录吗',[            {text:'取消'},            {text:'确定',onPress:()=>{                //点击确定之后清空cookie                Cookies.remove('userid')                // 并且清除redux中的user信息                this.props.resetMsg()                }}        ])}    render(){        const {username,info,salary,post,header,company} = this.props.user        console.log(header)        return(            <div style={{marginBottom:50,marginTop:45}}>                <Result                img={<img src={require(`../../assets/images/${header}.png`)} style={{width:50}}/>}                alt='header'                title={username}                message={company}                />                <List renderHeader={()=> '相关信息'}>                    <Item multipleLine>                        <Brief>职位:{post}</Brief>                        <Brief>简介: {info}</Brief>                        {salary?<Brief>薪资:{salary}</Brief>:null}                    </Item>                </List>                <WhiteSpace/>                <List>                    <Button type='warning' onClick={this.loginOut}> 退出登录 </Button>                </List>            </div>        )    }}export default connect(    state =>({user : state.user}),{resetMsg})(Personal)