module.exports = function (server) {    //socket.io是一个函数 函数传入参数 得到io对象    // 将它挂在服务器上server 从www里面传过去的    const io = require('socket.io')(server)    // 监听连接 当有一个客户连接上时回调 ，    // 有客户端和我连接的时候就会出发回调，socket当前服务器与浏览器连接对象        // io 表示任意浏览器        // socket 表示一个连接 绑定监视    io.on('connection',function (socket) {        console.log('socketio connected')        //绑定sendMsg监听，接收客户端发送的消息 回调函数 浏览器发来的信息        socket.on('sendMsg',function (data) {            console.log('服务器接收到浏览器的信息',data)            //向客户端发送消息（名称，数据）            io.emit('receiveMsg',data.name + '_' + data.date)            console.log('服务器向浏览器发送的消息',data.name + '_' + data.date)        })    })}